# Índices y vistas

**Pendiente de implementar.** Índices y vistas justificados por las consultas representativas:

- [ ] Vista `v_active_catalog`: catálogo activo con disponibilidad (consulta 1), si se consultara
      con frecuencia.
- [ ] Índice sobre `interaction_event (customer_id, event_type, occurred_at)`, para la consulta 3.
- [ ] Índice sobre `inventory (available_qty)`, para la consulta 4, si el volumen lo requiriera.
